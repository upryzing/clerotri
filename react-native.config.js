module.exports = {
  project: {
    ios: {},
    android: {},
  },
  // rename this to `assets` if you're working on iOS support; this was renamed to prevent `react-native-asset` from erroring, but the error is because the iOS code hasn't been touched/updated in a while
  androidAssets: ['./assets/fonts'],
};
