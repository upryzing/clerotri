import {type types} from '@react-native-documents/picker';

export const pickDocument = (_opts: any) => {
  console.warn('DocumentPicker is not implemented on web');
};

export const fileTypes = {} as typeof types;
